[tool.poetry]
name = "danio"
version = "0.1.0"
description = "ORM"
authors = ["MingyuWu <mingyu.wu@garena.cn>"]

[tool.poetry.dependencies]
python = "^3.7"
databases = "^0.4.3"

[tool.poetry.dev-dependencies]
pytest = "^4.6"
pytest-asyncio = ">=0.8.0"
pytest-cov = ">=2.5.1"
aiomysql = "^0.0.21"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
